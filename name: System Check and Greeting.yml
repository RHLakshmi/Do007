---
- name: System Check and Greeting
  hosts: localhost
  tasks:
    - name: Get the current uptime
      ansible.builtin.command: uptime
      register: uptime_output

    - name: Print the uptime result
      ansible.builtin.debug:
        msg: "The system status is: {{ uptime_output.stdout }}"
